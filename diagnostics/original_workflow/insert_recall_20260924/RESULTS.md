# 插入记录的完整结果验证（2026-09-24）

结论：在本轮 **6 个合成 L2 场景**中，`rnum_reset` 一行修复版的 49 次范围查询返回的 **8,243 个预期逻辑 ID 全部正确**，距离最大绝对误差 `6.50e-5`（阈值 `1e-3`），没有遗漏、额外 ID 或重复 ID。符合查询半径的插入记录命中 **112/112 次**；按各独立场景中的插入实例计，至少一次符合查询条件的记录为 **49/49 条**。这证明这些被测输入的插入召回率为 100%，不构成对任意输入的数学保证。

每个场景独立启动，数据沿用原有固定的 1,000 条互异、128 维整数向量。插入复制指定的原始向量，查询结果使用更新后当前逻辑 ID。独立 CPU 活动记录列表逐条计算整数平方 L2 真值，核对完整 ID 集合与距离，避免“漏一条、又多一条但总数不变”被计数检查掩盖。查询包括重建前缓冲、10 条触发重建、删除缓冲/已重建记录、半径 0、半径 1,200 和全包含半径 10,000。

| 场景 | 查询次数 | 预期结果 ID 数 | 符合半径的插入记录命中 | 完整结果 |
|---|---:|---:|---:|---|
| 缓冲区逐条同点查询 | 9 | 21 | 12/12 | 通过 |
| 缓冲区删除 | 5 | 9 | 4/4 | 通过 |
| 插入中查询、重建、再删除 | 16 | 38 | 22/22 | 通过 |
| 不先查缓冲而重建 | 8 | 18 | 10/10 | 通过 |
| 全包含查询 | 5 | 5,031 | 31/31 | 通过 |
| 部分半径查询 | 6 | 3,126 | 33/33 | 通过 |

重建前命中 36/36，重建后命中 76/76；发生删除后的查询命中 37/37。上表的 112 是“查询 × 符合条件的插入实例”对，同一实例在不同查询中会重复计入。6 个独立场景共执行 50 次插入，其中 1 条在其场景的部分半径查询中始终不落入半径，因此按至少一次符合条件的实例计为 49 条。

保留原版作少量对照：`buffer_exact`、`rebuild_control` 的完整结果通过；`rebuild_exact` 在先查询缓冲区再重建后失败。首次偏离在第 19 个操作，预期 3 个 ID，实际 5 个，额外出现越界 ID `1010`、`1018`。原版这个场景的 22/22 个应命中的插入结果仍能找到，但额外结果使完整查询不正确；随后查询重复出现该错误。一行修复版同场景完整通过。这个观察与此前确认的旧 `rnum[0]` 计数未清零一致。

在 GPU3（`GPU-a149f5af-55ab-ce33-8d3d-371a7ae61dd2`）运行；每次按现有 runner 检查空闲、持有设备锁并记录前后状态。所有被测程序均正常退出，无记录到的 CUDA 运行错误或外来 GPU 活动。修复版的 `rebuild_exact` 另经 memcheck、synccheck 各一次，均报告 `ERROR SUMMARY: 0 errors`，完整 ID 核对仍通过。审计副本只在已有 GPU 同步之后输出结果，原版及修复版各自的 8 个源文件逐项哈希核验，50 个 GPU kernel 函数体哈希未变；未用审计运行解释端到端时延。

可复核摘要及各次二进制、输入、日志哈希见 [EVIDENCE.json](EVIDENCE.json)。[prepare_recall.py](prepare_recall.py) 生成固定输入和仅增加 6 行主机输出的审计副本；复用原有 `run.py` 执行并由 [check_recall.py](check_recall.py) 对完整结果重新判定。原始日志、输入、审计源及运行记录保存在本地忽略目录 `diagnostics/original_workflow/local/insert_recall_evidence.tgz`，SHA-256 为 `697189e7027151a978a8932c8aaed8c163349445dc8c7a83105fe2b1d340f926`。

边界：仅验证这些确定性整数 L2 数据和查询；查询向量取未删除的原始行，未验证以尚在缓冲区的插入行作为查询 ID、任意删除模式、其他距离度量、并发或规模外推。对“任意插入都百分百可查”仍不能下普遍结论。源码基线为 `ZJU-DAILY/GTS@3bac1b725e92e98e3b69d5cb79ad9c56ccb5e639`，成果提交目标仅为 `wangwenqingqq/GTS-variants`。
